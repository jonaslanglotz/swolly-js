/**
 * Enum describing fields of {@link Category} by which items may be sorted.
 */
const CategorySortField = {
    ...require("./sortField"),
    NAME: "name",
}

module.exports = CategorySortField

