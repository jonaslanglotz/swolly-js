/**
 * Enum describing fields of any {@link Model} by which items may be sorted.
 */
const SortField = {
    ID: "id",
    CREATED: "createdAt",
    UPDATED: "updatedAt",
}

module.exports = SortField

